<div id="forgot-password" v-cloak>
  <div class="container">
    <h1 class="text-center">Recuperar Senha</h1>
    <div class="forgot-form" v-if="!cloudSuccess">
      <p class="text-center">Informe seu endereço de e-mail abaixo para resetar a senha de sua conta.</p>
      <hr/>
      <ajax-form action="sendPasswordRecoveryEmail" :syncing.sync="syncing" :cloud-error.sync="cloudError" @submitted="submittedForm()" :handle-parsing="handleParsingForm">
        <div class="form-group">
          <input type="email" name="email-address" class="form-control" placeholder="fulano@icomp.ufam.edu.br" :class="[formErrors.emailAddress ? 'is-invalid' : '']" v-model.trim="formData.emailAddress" autocomplete="email" focus-first>
          <div class="invalid-feedback" v-if="formErrors.emailAddress">Por favor, informe um endereço de e-mail válido.</div>
        </div>
        <p class="text-danger" v-if="cloudError"><small>An error occured while processing your request. Please check your information and try again, or <a href="/contact">contact support</a> if the error persists.</small></p>
        <div class="form-group">
          <ajax-button type="submit" :syncing="syncing" class="btn btn-dark btn-lg btn-block">Resetar Senha</ajax-button>
        </div>
      </ajax-form>
      <p class="text-center"><a href="/login">Voltar ao Login</a></p>
    </div>
    <div class="success-message" v-if="cloudSuccess">
      <p>Enviamos um link de atualização de senha para o e-mail informado.</p>
      <hr/>
      <p>Se o e-mail não chegar após alguns minutos, tente checar sua caixa de spam. Se ainda não conseguir encontrá-lo, tente novamente, ou <a href="/contact">contate o suporte</a>.</p>
      <p class="text-center"><a class="btn btn-outline-primary" href="/login">Back to login</a></p>
    </div>
  </div>
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>
